name: Our workflow

on: 
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: Checkout the latest version
      - run: |
          write-console "yay 🎉"
        shell: pwsh
